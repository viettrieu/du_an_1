<?php
use DB;
    class doiMatKhau extends DB{
        $arr = [];
        // select `email1`,`fullname` form user where email = '$post['email']'
        $arr['email'] = $user['email'];
        $arr['email'] = $user['fullname'];
        if(isset($user['email']) && )
    }